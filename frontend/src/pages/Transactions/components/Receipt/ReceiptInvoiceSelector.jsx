/*
 * Not wired up.
 *
 * The current Transactions.jsx doesn't let a receipt be
 * linked to a specific outstanding sales invoice - it's
 * just a flat Customer + Amount + Account entry. This
 * file is a placeholder if/when you add "receipt against
 * invoice" selection (mirroring PaymentBillSelector).
 */
export default function ReceiptInvoiceSelector() {
    return null;
}
